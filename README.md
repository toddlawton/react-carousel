* Simple React.js Carousel
@toddlawton

Install instructions
--
* Clone this repo
* Run npm install

Dev server
-- 
* Demo this on local by running: 
```
$ npm install -g webpack-dev-server
$ webpack-dev-server --inline --hot
```
* Visit http://localhost:8080/